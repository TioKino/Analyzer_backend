# API Configuration
# =================
# DEPRECATED: Este archivo ya NO contiene tokens.
# Todos los tokens se gestionan via variables de entorno (config.py).
# Ver config.py para la lista completa de variables requeridas.

import os

# Re-exportar desde config.py para compatibilidad con imports existentes
from config import AUDD_API_TOKEN, DISCOGS_TOKEN, MUSICBRAINZ_USER_AGENT
